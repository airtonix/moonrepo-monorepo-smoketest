# {{ name }}

TypeScript Cloudflare Worker service scaffold.

## Tasks

```bash
moon run {{ name }}:build
moon run {{ name }}:package -- latest preview
moon run {{ name }}:publish -- latest preview
```

Health endpoint: `GET /health`
